# --- Kraken Futures (CCXT id: krakenfutures) ---
# Use keys with futures permissions. CCXT expects apiKey + secret (HMAC) unless you use a custom signer.
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# Optional aliases (same values as above):
# KRAKEN_FUTURES_API_KEY=
# KRAKEN_FUTURES_API_SECRET=
